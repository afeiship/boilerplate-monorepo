{
  "name": "boilerplate-monorepo",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "build": "lerna run build --since master",
    "publish": "npm run build && lerna publish"
  }
}